# 인수인계 — 이 자료가 어떻게 만들어졌나

영어 52권으로 만든 것을 **다른 과목(수학 등)으로 다시 만들 때** 읽으라고 쓴 문서다.
결과물이 아니라 **왜 그렇게 했는지와 어디서 깨졌는지**를 남긴다. 같은 데서 두 번 깨지지 말라고.

- 만든 것 → https://themoka7.github.io/myChildrenEngStudy/
- 저장소 → `themoka7/myChildrenEngStudy`, 기본 브랜치 `claude/skill-utilization-6jr45t`
- 대상 아이 → 초4, 파닉스 초급, 하루 20분

---

## 1. 무엇을 만들었나 — 세 겹 구조

| 층 | 파일 | 누가 보나 | 무엇 |
|---|---|---|---|
| **지도** | `index.html` | 아이 | 52권이 테마별 구역으로 길에 늘어서 있다. 한 권 끝내면 칸에 **도장**. 구역을 다 채우면 색종이가 쏟아진다 |
| **읽기** | `read/unitNN.html` | 아이 | 단어 툴팁(품사·발음·뜻), 문장별 해석 토글, 🔊 듣기, 단어 퀴즈 |
| **문제지** | `print/unitNN.html` | 종이 | A4 3쪽 — 문제 2장 + **부모용 정답·지도 노트 1장** |
| 목록 | `list.html` | 부모 | 학습 날짜·걸린 시간·상태 필터 |
| 안내 | 지도 우상단 「📋 안내 보기」 | 처음 온 사람 | 20분 진행 순서, 문항 구성, 정답지 설명 |

지도 칸 → 「읽으러 가기 · 문제지 뽑기 · 도장 쾅! 찍기」.

**이 구조가 핵심이다.** 과목이 바뀌어도 그대로 쓸 수 있다.
아이 화면(지도)과 관리 화면(목록)을 **반드시 분리할 것** — 진도표를 지도에 합치면 아이 화면이 관리 화면이 된다.

---

## 2. 저장소 구조

```
units.json            단 하나의 원본. 52권의 번호·이름·한글·그림·상태·날짜·자료유무
data/unitNN.read.json   읽기 페이지용 데이터 (손으로 씀)
data/unitNN.quiz.json   문제지용 데이터 (손으로 씀)
read/  print/            생성물. 직접 고치지 말 것 — 다시 빌드하면 덮어쓴다
index.html list.html     생성물
tools/
  build_map.py     units.json → index.html (지도 + 안내 패널)
  build_index.py   units.json → list.html  (부모용 목록)
  build_unit.py    data/unitNN.*.json → read/ + print/   ★ 스킬 빌더를 호출하는 곳
  check_data.py    전수 검사. 커밋 전에 반드시
  where_overflow.py  A4가 넘칠 때 어느 쪽인지 짚어 줌
```

**원본은 JSON, HTML은 전부 생성물.** 이 원칙을 깨지 말 것. HTML을 손으로 고치기 시작하면
52장을 다시 만들 수 없게 되고, 그러면 팔레트 하나 바꾸는 것도 못 하게 된다.
실제로 이 프로젝트는 도중에 **읽기 52장을 두 번, 문제지 52장을 한 번** 통째로 다시 만들었다.

---

## 3. 스킬을 어떻게 썼나

두 스킬의 빌더를 **고치지 않고 그대로 호출**하고, 결과물에 사이트 내비게이션과 팔레트만 덧입힌다.

```python
# tools/build_unit.py
run([sys.executable, f"{skills}/eng-make-question/scripts/build.py", src, "-o", out, "--check"])
inject(out, READ_CSS, '<div class="toolbar">', nav_html)   # </head> 앞에 <style>, 툴바 뒤에 링크
```

`inject()` 는 붙일 자리를 못 찾으면 **조용히 넘어가지 않고 죽는다.** 스킬 템플릿이 바뀌면
즉시 알아야 하기 때문이다.

색을 바꿀 때도 템플릿을 고치지 않았다. `:root` 변수를 덮어쓰는 `<style>` 을 **뒤에** 끼워 넣어
이기게 하고, 템플릿이 하드코딩해 둔 색 몇 군데만 따로 짚었다.

> **수학용으로 옮길 때** — `eng-make-question` 은 영어 전용이다(파닉스·단어 뜻 연결).
> 문항 종류는 새로 짜야 한다. 하지만 **A4 3쪽 골격·정답지 철학·레이아웃 검증 방식**은 그대로 베낄 것.
> `skill-creator` 스킬로 `math-make-question` 같은 걸 새로 만드는 게 정공법이다.

---

## 4. 데이터 스키마

### units.json
```jsonc
{
  "course": "EBS 초등 영어 읽기",
  "subtitle": "영어 모험 지도",        // 이름. 여기만 고치면 전부 따라온다
  "groups": [
    { "theme": "동물", "ico": "🐾", "units": [
      { "n": 1, "en": "Rabbits", "ko": "토끼", "ico": "🐰",
        "status": "done",            // done | doing | todo
        "date": "26.08.19", "min": 14,
        "ready": true }              // 읽기+문제지가 만들어졌는가
    ]}
  ]
}
```

### quiz.json (문제지)
```jsonc
{
  "title": "...", "title_ko": "...",
  "paras": [["문장1.", "문장2."], ["문장3."]],   // 문단 안의 문장 배열 = 문장 번호의 근거
  "match": [{"en":"hop","ko":"폴짝 뛰다"}],       // ① 단어-뜻 연결
  "match_ko_order": ["...", "..."],              // 반드시 섞인 순열
  "phonics": [{"q":"h_p","a":"o","ko":"폴짝"}],  // ② 빠진 모음
  "scan": [{"q":"한글 질문","a":"영어 답","sent":"7"}],   // ③ 찾아 쓰기
  "tf": [{"en":"...","answer":"O","why":"...","sent":"4","gloss":"only 오직"}],  // ④ O/X
  "fill_bank": ["a","b","c"], "fill": [{"en":"... ___ ...","a":"a","ko":"..."}], // ⑤ 빈칸
  "trace": [{"en":"...","ko":"..."}],            // ⑥ 따라 쓰기
  "routine": ["<b>0~3분</b> ..."],               // 정답지 아래 20분 진행 순서
  "notes": {"phonics":"...","scan":"...","tf":"...","trace":"..."}  // 부모용. HTML 허용
}
```

**`notes` 만 HTML 이 살아 있고 나머지 필드는 전부 이스케이프된다.** `why` 에 `<b>` 를 쓰면
정답지에 태그가 글자로 찍힌다. (실제로 당했다 — 5장 참고)

---

## 5. 문항을 만드는 원칙 (과목이 바뀌어도 유효한 것)

- **답이 뻔한 문항은 문항이 아니다.** "주어에 동그라미" 같은 건 맨 앞에 치면 100점이다.
  지시문이 *이해*를 요구하는지, *위치 규칙*만 알면 풀리는지 스스로 검사할 것.
- **정답지에 정답만 있으면 반쪽이다.** 아이가 틀렸을 때 부모가 뭐라고 해야 할지 모르면
  그 자리에서 공부가 멈춘다. **그대로 읽으면 되는 문장**으로 적을 것.
  → `notes` 가 이 프로젝트에서 가장 값진 부분이다.
- **형식을 고정한다.** 매번 같은 순서의 6문항. 아이가 "뭘 하라는 거지"에 힘을 안 쓰게.
- **오답은 아이가 실제로 하는 실수에서 가져온다.** 예: 4개 행성 지문이 거의 똑같아서
  각 행성의 거짓 문장을 *옆 행성에서* 빌려 왔다. 헷갈리는 게 진짜 실수니까.
- **유닛을 가로질러 연결한다.** 배(bow/stern)와 비행기(nose/tail)에 같은 유형의 함정을 놓고
  정답지에 "다음 유닛에도 같은 문항이 나온다"고 예고 → 아이가 규칙을 스스로 발견한다.
- **민감한 내용**(링컨 암살 등)은 지문에 두되 **문항을 만들지 않고** 정답지에 부모용 경고를 단다.

---

## 6. 값비싼 실수들 — 여기서 두 번 깨지지 말 것

### 6.1 A4는 넘쳐도 조용히 사라진다 ★★★
문제지의 `.page` 가 **높이 고정 + `overflow:hidden`** 이다. 내용이 넘치면 쪽이 늘어나는 게 아니라
**그냥 지워진다.** 눈으로 안 보인다.
→ `build_unit.py` 가 weasyprint 로 실제 렌더링해서 3쪽인지·잘림이 없는지 검사하고, 넘치면 **빌드를 실패시킨다.**
→ 어느 쪽이 넘쳤는지는 `where_overflow.py` 가 높이를 풀어서 다시 렌더링해 짚어 준다.
→ 덜어낼 순서: ③(찾아 쓰기)은 마지막까지 남기고, **다른 문항과 단어가 겹치는 것부터** 뺀다.
   운문·대화체는 문장이 길어서 1쪽이 아니라 **2쪽**이 넘친다.

### 6.2 스킬 빌더의 기본값이 그대로 나갔다 ★★★
정답지 「20분 진행 순서」 1번이 **52장 전부** `/h/-/o/-/p/ → hop` 이었다. `hop` 은 1권에만 있는 단어다.
빌더가 `d.get('routine')` 으로 읽는데 데이터에 `routine` 을 안 채워서 예시 지문의 기본값이 나간 것.
→ **빌더에 기본값이 있는 필드를 전부 훑어볼 것.** 안 채우면 남의 데이터가 나간다.
→ 지금은 `build_unit.py` 가 `phonics[0]` 에서 자동 생성한다. 손으로 채우면 또 어긋난다.

### 6.3 이스케이프되는 필드에 태그를 썼다
`notes` 만 HTML 이 살아 있다. `tf.why` 에 `<b>` 를 넣었더니 정답지에 태그가 글자로 찍힐 뻔했다.
→ `check_data.py` 가 이제 이스케이프되는 모든 필드에서 `<` 를 거부한다.

### 6.4 휴대폰에는 hover 가 없다
읽기 페이지의 듣기·해석 버튼이 `opacity:.35` 로 깔려 있고 `.line:hover` 일 때만 살아났다.
**폰에서는 계속 흐린 채로 남는다.** 아이가 안 눌리는 것처럼 보이는 버튼을 누르고 있었다.
→ hover 로만 드러나는 UI 는 만들지 말 것. `@media (hover:none)` 으로 받쳐 줄 것.

### 6.5 검사기가 0개를 찾고 통과했다
링크 검사가 `href="read/..."` 를 훑는 방식이었는데, 지도는 링크를 **자바스크립트가 만든다.**
정규식이 0개를 찾고 "이상 없음"으로 통과했다.
→ **검사기가 아무것도 못 찾았을 때 통과시키지 말 것.** 개수를 기대값과 대조할 것.
→ 검사기를 고친 뒤 반드시 **일부러 깨뜨려서 실패하는지** 확인할 것. (페이지 하나를 치워 보는 식으로)

### 6.6 형광펜이 어두운 배경에서 글자를 반만 보이게 했다
`mark::before` 로 글자 **아래쪽 55%** 만 노란 띠로 덮는 방식이라, 어두운 칸에서는 띠 위로 올라간
글자 윗부분이 배경에 묻혔다. 또 `z-index:-1` 이라 배경 있는 컨테이너에 넣으면 띠가 뒤로 숨는다.
→ `isolation:isolate` 로 mark 자체를 쌓임 문맥으로 만들 것. 어두운 데서는 띠를 글자 높이만큼 채울 것.

### 6.7 CSS 를 합칠 때 일반 선택자가 남의 배치를 부순다
안내 패널을 지도에 합칠 때 원본의 `section{padding:72px 0}` 하나로 지도가 무너질 뻔했다
(지도의 구역도 `<section>`).
→ 남의 페이지를 합칠 때는 **모든 규칙에 `#scope` 를 붙일 것.** 색 변수도 그 안에서만 살게.

### 6.8 GitHub 권한 — Authorize 와 Install 은 다르다 ★★★
푸시가 계속 403이었다. `git push` · MCP · 새 세션 전부 실패. 원인은 GitHub App 이
**Authorize(신원 확인)만 되고 Install(저장소 권한)이 안 된 것.**
→ https://github.com/apps/claude 에서 **Install** 까지 해야 한다.
→ 진단: `curl .../info/refs?service=git-receive-pack` 이 403이고 `git-upload-pack` 이 200이면 이 경우다.

### 6.9 Pages 는 손으로 한 번 켜야 한다
워크플로에 `enablement: true` 가 있어도 워크플로 토큰에는 Pages 를 처음 켤 권한이 없다.
`Resource not accessible by integration` 으로 실패한다.
→ Settings → Pages → Source: **GitHub Actions**. 한 번만.
→ 빈 저장소에 처음 푸시한 브랜치가 기본 브랜치가 되고, Pages 는 기본 브랜치에서만 배포된다.

### 6.10 원문 오타
EBS 지문 전사본에 오타가 많았다. 50군데 가까이 고쳤다.
`hid dog`→`his`, `wake up`→`woke`, `Mars in very dry`→`is`, 전각 `！（）`, 빠진 물음표…
**가장 위험했던 것**: 화성 거리가 `412 million miles (229 Million km)` 이었는데 229M km = **142M** miles 다.
자릿수가 뒤집힌 것. 아이가 그대로 베껴 답으로 쓰고 외우는 숫자라 반드시 잡아야 한다.
→ **숫자는 단위 환산으로 검산할 것.**

---

## 7. 도장(진도) 저장 방식

두 군데에서 온다.

- `units.json` 의 `status:"done"` → 지도에 **미리 찍혀 있는 도장**. 저장소 기록이라 어느 기기에서나 같다.
- 브라우저 `localStorage` → 아이가 **직접 찍은 도장**. 그 기기에만.

저장할 때 **그때의 씨앗 목록을 함께 남겨** 두고, 다음에 열 때 그 사이 새로 완료로 바뀐 것만 더한다.
그래서 `units.json` 을 나중에 고쳐도 **아이가 일부러 뺀 도장이 되살아나지 않는다.**

```js
const o = JSON.parse(localStorage.getItem(KEY));
done = new Set(o.done);
const seen = new Set(o.seed);
SEED.forEach(n => { if (!seen.has(n)) done.add(n); });   // 새로 늘어난 씨앗만
```

기기 사이 동기화는 안 된다. 「진도 복사하기」로 목록을 뽑아 `units.json` 에 옮겨 적는 게 유일한 길이다.
`localStorage` 접근 자체가 막힌 브라우저도 있으니 **읽기·쓰기를 전부 try/catch 로 감싸고**,
저장이 안 되면 화면에 알릴 것.

---

## 8. 팔레트 (세 페이지가 한 덩어리로 보이게)

```
--paper #E8F0F7   모눈종이 파랑 (26px 격자)
--ink   #2A3F55   남색 — 글자·테두리
--line  #B9CDDD   연한 선
--stamp #E0384F   도장 빨강 — 강조·완료
--gold  #F2B632   금색 — 주 버튼
글꼴    Jua(제목·버튼) · Gaegu(손글씨체 보조) · 지문은 세리프 유지
모양    테두리 2.5~3px + `box-shadow: 0 3px 0` (아래로 떨어지는 그림자)
```

**지문 글꼴은 둥글게 만들지 말 것.** 읽기가 목적인 자리는 읽기 좋은 글꼴로 둔다.

---

## 9. 아직 남은 일 (영어 쪽)

다른 세션(Claude)이 52장을 전수 검토했고, 확인 결과 아래가 사실이었다.

| 항목 | 실측 | 상태 |
|---|---|---|
| 정답지 워밍업이 전부 `hop` | 52/52 | **고침** |
| 파닉스가 다음절 (`hospitals`, `medicine`) — 스킬 규칙은 CVC/CVCC 단모음 | 60건 | 미해결 |
| 파닉스에 고유명사 (Tom·Fran·Jack·Max·Ben) | 5건 | 미해결 |
| O/X 문장이 지문 그대로 → "본 문장이면 O" 요령으로 뚫림 | **20유닛이 만점** | 미해결 |
| ⑤ 미사용 보기가 다른 문항 정답과 겹침 | 20유닛 | 미해결 |
| ③ 정답이 구(phrase)라 영어 못 하는 부모가 채점 불가 | 67건 | 미해결 |

> **검토 결과를 그대로 믿지 말 것.** 그 문서는 "인덱스에 13개 누락"(이미 해결됨),
> "O/X 45유닛 재작성 필요"(실제로 뚫리는 건 20유닛), "보기 겹침 32유닛"(실제 20) 처럼
> 틀리거나 부풀려진 항목이 섞여 있었다. **숫자는 직접 재 볼 것.**

O/X 를 잴 때 쓴 방법이 쓸 만하다 — "지문에 있는 문장이면 O, 아니면 X" 라는 요령만으로 몇 점이
나오는지 계산한다. 만점이 나오면 그 유닛은 읽지 않고도 풀린다는 뜻이다.

**그리고 가장 중요한 지적**: 자료는 52권인데 아이는 13권에 있고 39일 멈춰 있었다.
**자료를 더 만드는 것보다 아이와 다음 한 권을 하는 게 우선이다.**

---

## 10. 수학으로 옮길 때

### 그대로 가져갈 것
- 세 겹 구조(지도 → 학습 화면 → A4 문제지)와 아이/부모 화면 분리
- 도장·구역·색종이 (동기 부여 장치는 과목과 무관하다)
- `units.json` 단일 원본 + 전부 생성물 원칙
- A4 3쪽 = 문제 2 + **부모용 정답·지도 노트 1**
- 레이아웃 넘침을 **빌드 실패로** 잡는 방식
- `check_data.py` 식 전수 검사 — 한 번 깨진 것은 전부 검사 항목으로 승격
- 5장의 문항 원칙 전부

### 새로 만들어야 할 것
- **문항 종류.** 영어의 6문항(단어연결·파닉스·찾아쓰기·O/X·빈칸·따라쓰기)은 수학에 안 맞는다.
  수학이라면 예컨대 ① 개념 연결 ② 계산 연습 ③ 그림·표에서 읽어내기 ④ 참/거짓 판단
  ⑤ 빈칸 있는 식 ⑥ 서술형 한 문제 — 같은 식으로 **고정 6문항을 새로 설계**할 것.
- **학습 화면.** 읽기 페이지의 단어 툴팁·TTS 는 언어 전용이다. 수학은 풀이 단계 펼치기,
  도형 조작, 오답 확인 쪽이 맞다.
- **채점 가능성이 더 중요해진다.** 영어에서도 "부모가 채점할 수 있는가"가 문제였는데
  (③번 정답이 구라서 애매), 수학은 **답이 하나로 떨어지므로 오히려 유리하다.** 이 장점을 살릴 것.
- 파닉스에 해당하는 "매일 하는 준비운동"이 수학에도 필요하다 — 구구단·수 세기 같은 것.

### 작업 순서 (영어에서 통했던 것)
1. 한 유닛으로 끝까지 만들어 본다 (데이터 → 빌드 → 레이아웃 검증 → 배포)
2. 그 다음 3~5개씩 묶어서 진행. 묶음마다 커밋·푸시·배포 확인
3. 깨질 때마다 **검사기에 항목을 추가**한다. 사람이 기억하게 두지 말 것
4. 목차·지도는 마지막이 아니라 처음에 만든다. 진행 상황이 보여야 한다

---

## 11. 명령어

```bash
python3 tools/build_unit.py NN     # 유닛 하나: read/ + print/ (레이아웃 검증 포함)
python3 tools/build_map.py         # 지도 index.html
python3 tools/build_index.py       # 목록 list.html
python3 tools/check_data.py        # 전수 검사 — 커밋 전 필수
python3 tools/where_overflow.py print/unitNN.html   # A4 넘칠 때
```

배포는 `main` 또는 `claude/**` 에 푸시하면 GitHub Actions 가 저장소를 그대로 Pages 에 올린다.

---

## 부록 — 이 대화의 원본

전체 대화 기록은 세션 로그에 있지만 **12MB** 라 다른 세션에 넘기기엔 크다.
52개 지문 원문과 빌드 로그가 대부분이다. 이 문서가 그중 남길 가치가 있는 것을 추린 것이다.
